/* ========================================= */
/* 🌙 1. 颜色变量定义（全站统一） */
/* ========================================= */

:root {
  /* 亮色模式：淡蓝 -> 白色 渐变 */
  --bg-color-start: #cfe1f7ff;
  --bg-color-end: #ffffff;
}

/* 🌙 暗黑模式：纯黑 -> 纯黑 渐变（实现纯黑色背景） */
/* 匹配您的 JS 逻辑：当 HTML 具有 color-scheme="dark" 属性时生效 */
html[color-scheme="dark"] { 
  --bg-color-start: #121212; /* 纯黑色 */
  --bg-color-end: #121212;  /* 也是纯黑色 */
}


/* ========================================= */
/* 🖼️ 2. 全站背景应用 (使用变量和固定视角) */
/* ========================================= */

html {
  /* 渐变本身：使用变量 */
  background-image: linear-gradient(180deg, var(--bg-color-start) 0%, var(--bg-color-end) 100%);
  
  /* 确保html元素至少有视口的高度 */
  min-height: 100vh; /* 使用 vh 确保至少铺满视口 */
  
  /* 关键：防止渐变重复 */
  background-repeat: no-repeat;
  
  /* 关键：让背景固定在视口中，不随滚动条滚动 */
  background-attachment: fixed; 
  
  /* 关键：让背景拉伸至覆盖整个视口，即使内容很短 */
  background-size: 100% 100%; /* 宽度100%，高度100% */
}


/* ========================================= */
/* 💧 3. 透明化容器 */
/* ========================================= */

body {
    /* 确保 body 清理干净，不遮挡 html 上的背景 */
    background: transparent !important;
    min-height: 100%;
    margin: 0;
}

/* 透明掉主要容器背景，让渐变真正可见 */
#safearea,
.body-wrapper,
#l_main,
#l_side,
#l_body {
    background: transparent !important;
}